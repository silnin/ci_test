version: 2
jobs:
  build:
    docker:
      - image:  circleci/php:7.0-apache-node-browsers
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date